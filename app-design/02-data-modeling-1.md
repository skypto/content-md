separatating your data from it's visual presentation.  
  
storing in different ways makes different things easier.  
  
exercise: create a few simple data storing object app  
	* store in object  
	* store in array  
	* model as variables
	* model as objects